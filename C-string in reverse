#include <stdio.h>
#include <string.h>

int main(void)
{
	char text[] = "Learning about pointers.";
	char* ptr = 0;
	ptr = text + strlen(text)-1;
	while (ptr >= text){
	    printf("%c", *ptr);
	    ptr--;
	}
	return 0;
}
